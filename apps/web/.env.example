WATCHPACK_POLLING=true
# UsefulCRM environment variables

# URL of the UsefulCRM API Bridge (api-bridge/main.py)
# The bridge is a standalone FastAPI server backed by DuckDB + Hermes agent.
# Run: cd api-bridge && pip install -r requirements.txt && python main.py
USEFUL_API_URL=http://localhost:3200
